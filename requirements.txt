# Direct dependencies, pinned to the versions used for this project.
# (Transitive dependencies are resolved automatically by pip.)
gymnasium==1.3.0
numpy==2.4.6
pygame==2.6.1
matplotlib==3.10.9
torch==2.12.0
